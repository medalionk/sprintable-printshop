import {connect} from 'react-redux';
import {bindActionCreators} from 'redux';
import PrintShopView from './PrintShopView';

const mapStateToProps = (state, ownProps) => ({

});

const mapDispatchToProps = (dispatch) => ({
  
});

export default connect(
   mapStateToProps,
   mapDispatchToProps
)(PrintShopView);
